<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.IMCAssistant" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryVariant">@color/primaryColor</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/secondaryColor</item>
        <item name="colorSecondaryVariant">@color/secondaryDarkColor</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <!-- No action bar theme -->
    <style name="AppTheme.NoActionBar" parent="Theme.IMCAssistant">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
    </style>

    <style name="AppThemeElement" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryDark">@color/primaryDarkColor</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="SplashTheme" parent="AppTheme.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_drawable</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="Theme.IMCAssistant">
        <item name="android:textColorSecondary">@android:color/white</item>
    </style>

    <style name="MaterialButton" parent="Widget.MaterialComponents.Button">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="backgroundTint">@color/primaryColor</item>
        <item name="android:textSize">@dimen/primary_button_text_size</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:padding">12dp</item>
        <item name="elevation">8dp</item>
        <item name="cornerRadius">25dp</item>
        <item name="android:textColor">@color/white</item>
    </style>


    <style name="CustomTextInputLayout" parent="Widget.Design.TextInputLayout">
        <item name="android:textSize">@dimen/edit_text_text_size</item>
        <item name="android:textColorHint">@color/hint</item>
        <item name="android:textColor">@color/text</item>
        <item name="hintTextAppearance">@style/TextInputLayoutHintText</item>
    </style>

    <style name="TextInputLayoutHintText" parent="TextAppearance.Design.Hint">
        <item name="android:textColor">@color/hint</item>
        <item name="android:textSize">@dimen/edit_text_text_size</item>
    </style>

    <style name="TextInputLayoutSuffixText">
        <item name="android:textColor">@color/text</item>
        <item name="android:textSize">@dimen/edit_text_text_size</item>
    </style>

    <style name="CustomTextInputEditText" parent="Widget.AppCompat.EditText">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textSize">@dimen/edit_text_text_size</item>
        <item name="android:textColor">@color/text</item>
        <item name="android:textColorHint">@color/hint</item>
        <item name="android:singleLine">true</item>
        <item name="android:maxLength">3</item>
    </style>

    <style name="DefaultTextInputEditText" parent="CustomTextInputEditText">
        <item name="android:inputType">text</item>
        <item name="android:digits">@string/digits_default_characters</item>
    </style>

    <style name="StringTextInputEditText" parent="CustomTextInputEditText">
        <item name="android:inputType">text</item>
        <item name="android:digits">@string/digits_only_letters</item>
    </style>

    <style name="EmailTextInputEditText" parent="CustomTextInputEditText">
        <item name="android:inputType">textEmailAddress</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:digits">@string/digits_email</item>
    </style>

    <style name="NumberTextInputEditText" parent="CustomTextInputEditText">
        <item name="android:inputType">number</item>
        <item name="android:digits">@string/digits_only_numbers</item>
    </style>

    <style name="DecimalTextInputEditText" parent="CustomTextInputEditText">
        <item name="android:inputType">numberDecimal</item>
        <item name="android:digits">@string/digits_numbers_decimal</item>
    </style>

    <style name="AlphaNumericTextInputEditText" parent="CustomTextInputEditText">
        <item name="android:inputType">text</item>
        <item name="android:digits">@string/digits_alphanumerics</item>
    </style>
</resources>